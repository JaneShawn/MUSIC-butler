# Core
pydantic>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Vector Database - 使用sqlite版本避免编译
chromadb>=0.4.0
sentence-transformers>=2.3.0

# Audio Processing - 可选，使用纯Python版本
# 如果安装失败可以先注释掉这些
mutagen>=1.47.0
librosa>=0.10.0; sys_platform != "win32" or python_version >= "3.9"

# HTTP & API
requests>=2.31.0
httpx>=0.26.0
python-dotenv>=1.0.0
feedparser>=6.0.10
# praw>=7.7.0  # Reddit支持（可选）

# Web界面
streamlit>=1.30.0

# Utils
tqdm>=4.66.0
watchdog>=3.0.0
